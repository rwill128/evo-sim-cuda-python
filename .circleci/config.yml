version: 2.1

orbs:
  python: circleci/python@1.2.1

jobs:
  build-and-test:
    executor: python/default
    steps:
      - checkout
      - python/install-deps
      - run:
          command: ./run_simulation/run_simulation_test.py
          name: Run Main Simulation

workflows:
  main:
    jobs:
      - build-and-test
